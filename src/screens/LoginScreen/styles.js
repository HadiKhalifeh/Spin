import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  main: {
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 95,
    flex: 1,
  },
  logo: {
    width: 120,
    height: 80,
    marginBottom: 50,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
